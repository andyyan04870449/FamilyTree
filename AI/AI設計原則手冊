# 📘 AI 寫代碼設計原則手冊（AI Coding Design Guidelines）

> 適用於：.NET Web 後端開發（AI協作模式）  
> 目的：讓 AI 在參與開發時，遵循統一的架構原則、安全原則與負責任的開發行為  

---

## 🎯 原則 1：集中管理硬代碼（Magic Values）

- 所有硬代碼應集中管理，避免散落於邏輯中。
- 包括：設定值、常數、Domain 字典、路由等。
- 設定應放入 `appsettings.json` 並透過 `IOptions<T>` 讀取。
- 常數應依語意區分為多個靜態類別。
- Domain 應使用 enum 或 value object 封裝。
- 路由統一管理於 Route 模組中。

---

## 🎯 原則 2：模組化與物件導向原則（避免重複，強化重用）

- 相同邏輯不得重複撰寫。
- 可重用邏輯應封裝為服務、Helper 或 Extension Method。
- 嚴格遵守單一責任原則（SRP）。
- 優先重用現有方法而非重新實作。
- 避免過度抽象與預支設計。

---

## 🎯 原則 3：遵守模組邊界，限制跨模組修改

- AI 僅能修改當前模組代碼，禁止擅自改動其他模組。
- 不得修改共用函式、Enum、Model、常數等非本模組擁有的代碼。
- 如判斷有依賴衝突，應回報並要求補全上下文。

---

## 🎯 原則 4：禁止擅自修改資料庫結構與欄位設計

- 禁止自行新增、移除、調整資料表或欄位。
- 所有 DDL 操作需經由人工或 DBA Agent 審批。
- 欄位屬於共享契約，修改前應分析所有依賴模組與回滾機制。
- AI 不得產出 `ALTER`, `DROP`, `RENAME`, `ADD COLUMN` 等 DDL 指令。

---

## 🎯 原則 5：面對問題，禁止逃避與掩蓋錯誤

- 不得將錯誤代碼註解或刪除以「略過錯誤」。
- 應分析錯誤原因，說明修正邏輯並保留紀錄。
- 若無法解決，應回報錯誤點與上下文需求，不得無聲略過。

---

## 🎯 原則 6：開發中應主動紀錄 Log 於關鍵位置

應於下列位置記錄 log 以利除錯與追蹤、稽核：

- 使用者行為（登入、修改密碼）
- 關鍵業務節點（下單、付款、驗收）
- 外部 API 或資料庫呼叫
- 例外處理區塊（try-catch）
- 權限驗證、登入失敗等存取控制
- 資料 CRUD 操作
- 批次任務啟動與完成
- 模型運算與推薦輸出

---

## 🎯 原則 7：資料庫查詢集中模組與中文註解標準

- 所有查詢應集中於資料存取模組（Repository / DAO）。
- 控制層或服務層不得自行查資料。
- 每一筆查詢應留下log。
- 所有查詢語句上方應加上中文註解，說明其用途與業務語意。

---

## 🎯 原則 8：堅守資訊安全原則（Security by Design）

- 禁止寫入任何敏感資訊（密碼、API Key）於代碼。
- 使用者輸入必須進行驗證與清洗，禁止直接拼接 SQL。
- 不可將完整 exception 資訊回傳給前端。
- 所有資源皆應依最小權限原則控管。
- Log 不得記錄個資、密碼、token 等機密資訊。
- AI 預設應自動加入權限驗證與風險檢查機制。

---

## 🎯 原則 9：違規操作需主動警示並經人工確認

- AI 應偵測並提示所有可能違反原則之行為。
- 若涉及高風險（如跨模組修改、DDL、刪除邏輯），應強調風險。
- 未經人工確認不得執行。
- 可提出安全替代方案供選擇。

---

> 📌 請所有 AI Agent 嚴格遵守以上各項原則。違反任何一條，應立即中止任務並回報使用者。